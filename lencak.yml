environment:
  LENCAK: "true"
inherit_environment: off
functions:
  diskusage:
    args: ["$PATH"]
    executor: ['/bin/sh', '-c']
    command: "du -sh $PATH"
  gitbranch:
    args: ["$PATH"]
    executor: ['/bin/sh', '-c']
    command: "git --git-dir $PATH/.git rev-parse --abbrev-ref HEAD"
